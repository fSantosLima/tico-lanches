// Auth configuration — full implementation in Task 6
// This stub allows route modules to import authOptions without errors.
// Tests mock this module via vi.mock('@/lib/auth', () => ({ authOptions: {} }))
export const authOptions = {}
